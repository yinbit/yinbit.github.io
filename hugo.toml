baseURL = 'https://yinbit.github.io/'
locale = 'en-us'
title = 'yinbitのblog'
theme = "PaperMod"


[params]
  defaultTheme = "auto"
  ShowReadingTime = true
  ShowShareButtons = true
  ShowPostNavLinks = true
[[menu.main]]
  identifier = "home"          # 唯一标识符，可用于多语言链接
  name = "🏠 首页"            # 显示在菜单上的名称
  url = "/"                   # 链接到的URL
  weight = 10                 # 排序权重，数字越小越靠前/靠左
